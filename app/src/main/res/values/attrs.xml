<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>
    <declare-styleable name="editTextStyleable">
        <!-- Our Custom variable, optionally we can specify that it is of integer type -->
        <attr name="focusedBackground" format="integer"/>
        <attr name="unFocusedBackground" format="integer"/>
        <attr name="focusedDrawable" format="integer"/>
        <attr name="unFocusedDrawable" format="integer"/>
        <attr name="focusedPaddingTop" format="dimension"/>
        <attr name="focusedPaddingBottom" format="dimension"/>
        <attr name="focusedPaddingStart" format="dimension"/>
        <attr name="focusedPaddingEnd" format="dimension"/>


    </declare-styleable>
</resources>
